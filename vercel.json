{
    "version": 2,
    "installCommand": "npm install --legacy-peer-deps",
    "buildCommand": "npm run clean && tsc --build && cp ./package.json ./dist/package.json && cd ./dist && npm install --legacy-peer-deps",
    "outputDirectory": "dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api"
        }
    ]
}